---
import { Image } from "astro:assets";
import photoperfil from "../../../../public/img/photo-perfil.webp";
---

<div class="content-presentation">
  <div class="linear-gradient-2"></div>
  <div class="linear-gradient-1"></div>

  <section class="container-persentation hero" id="inicio">
    <div class="container-text hero-glass">
      <h1 class="text-name">Franco Chacón</h1>

      <p class="p-profession">
        Data Analyst | Power BI, Excel & Business Intelligence Dashboards
      </p>

      <div class="presentation-text">
        <p>
          I design professional, interactive dashboards tailored to your business goals.
        </p>

        <p>
          I transform raw data into clear, actionable insights that improve performance, optimize operations, and support data-driven decisions.
        </p>

        <p>
          Whether you need a sales dashboard, financial report, marketing analytics, KPI tracking, or operational performance analysis, I build clean, dynamic, and customized solutions using Power BI or Excel.
        </p>

        <p><strong>What you can expect:</strong></p>

        <ul class="presentation-list">
          <li>Clear KPI structure aligned with your business goals</li>
          <li>Interactive filters and drill-down capabilities</li>
          <li>Data cleaning and modeling for accuracy</li>
          <li>Professional, executive-level design</li>
          <li>Easy-to-understand visualizations</li>
          <li>On-time delivery and responsive communication</li>
        </ul>

        <p>
          My goal is not just to create charts — it’s to build a decision-making tool that adds real value.
        </p>

        <p><strong>Let’s turn your data into insights.</strong></p>
      </div>

      <!-- CTA: usando los mismos IDs que tu navbar -->
      <div class="cta-row">
        <a class="btn btn-primary cta-link" data-home="#proyectos" data-project="#dashboard" href="#proyectos">
          View Projects
        </a>

        <a class="btn btn-ghost cta-link" data-home="#contacto" data-project="#contacto" href="#contacto">
          Contact
        </a>
      </div>
    </div>

    <div class="container-photo-profile">
      <div class="photo-perfil photo-float glass-ring">
        <picture>
          <Image src={photoperfil} alt="Foto de perfil de Franco Chacón" />
        </picture>
      </div>
    </div>
  </section>
</div>

<script>
  // Igual que el header: ajusta anchors si estás en /proyecto...
  const path = window.location.pathname;
  const isProjectPage = path.includes("/proyecto");

  document.querySelectorAll(".cta-link").forEach((a) => {
    const homeHref = a.getAttribute("data-home");
    const projectHref = a.getAttribute("data-project");
    a.setAttribute("href", isProjectPage ? projectHref : homeHref);
  });
</script>

<style>
  html {
    scroll-behavior: smooth;
  }

  .content-presentation {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .linear-gradient-1 {
    position: absolute;
    left: 0;
    top: 0;
    width: 500px;
    height: 520px;
    background: radial-gradient(
      at left center,
      rgba(1, 190, 150, 0.5),
      transparent,
      transparent
    );
    z-index: 0;
    pointer-events: none;
  }

  .linear-gradient-2 {
    position: absolute;
    right: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(
      at right center,
      rgba(1, 190, 150, 0.6),
      rgb(0, 0, 0),
      rgb(0, 0, 0)
    );
    z-index: 0;
    pointer-events: none;
  }

  .container-persentation {
    position: relative;
    display: flex;
    padding: 50px 0;
    width: 90%;
    margin: 0 auto;
    max-width: 1355px;
    z-index: 2;
  }

  .hero {
    min-height: 100vh;
    align-items: center;
    gap: 40px;
  }

  .container-text {
    width: 52%;
    color: #f6f6f9;
  }

  .hero-glass {
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-radius: 18px;
    padding: 22px;
  }

  .text-name {
    font-size: 34px;
    color: #01be96;
    font-weight: 800;
    letter-spacing: 1px;
    margin: 0;
  }

  .p-profession {
    font-size: 22px;
    font-weight: 800;
    margin: 6px 0 14px;
  }

  .presentation-text p {
    margin: 10px 0;
    line-height: 1.6;
  }

  .presentation-list {
    padding-left: 18px;
  }

  .cta-row {
    display: flex;
    gap: 12px;
    margin-top: 16px;
    flex-wrap: wrap;
  }

  .btn {
    padding: 10px 18px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    transition: 0.2s;
  }

  .btn-primary {
    background: #01be96;
    color: #000;
    border: 1px solid #01be96;
  }

  .btn-ghost {
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
    border: 1px solid rgba(1, 190, 150, 0.55);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .container-photo-profile {
    width: 48%;
    display: flex;
    justify-content: center;
  }

  .photo-perfil {
    height: 320px;
    width: 320px;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.45);
  }

  .glass-ring {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .photo-perfil img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .photo-float {
    animation: floaty 4.5s ease-in-out infinite;
  }

  @keyframes floaty {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @media (max-width: 900px) {
    .container-persentation {
      flex-direction: column-reverse;
      gap: 30px;
    }

    .container-text,
    .container-photo-profile {
      width: 100%;
    }

    .photo-perfil {
      height: 280px;
      width: 280px;
    }
  }

  @media (max-width: 500px) {
    .linear-gradient-1 {
      display: none;
    }
  }
</style>
